var assert = require('chai').assert;
var request = require('request');

describe('Fake Unit Tests', function(){
  it('True is equal to true', function(){
    assert.equal(true, true);
  });

  it('Earth is flat', function(){
    assert.equal(true, true);
  });

  it('Turing is cool', function(){
    assert.equal(true, true);
  });

  it('Pizza is yummy', function(){
    assert.equal(true, true);
  });

  it('Mean people are mean', function(){
    assert.equal(true, true);
  });

  it('Spinners gonna spin', function(){
    assert.equal(true, true);
  });
});
